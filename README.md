# My Blog — Платформа в стиле Threads

Этот проект представляет собой **блог-платформу, вдохновленную социальной сетью Threads**. Приложение разработано с использованием языка **Python** и веб-фреймворка Django, поддерживающего сложную логику взаимодействия пользователей и постов.

## 🛠 Технологический стек
Проект использует современный набор технологий для обеспечения высокой производительности и масштабируемости:
*   **Backend:** Python (Django)
*   **Frontend:** HTML, JavaScript, CSS
*   **Фоновые задачи:** Celery (с использованием Redis в качестве брокера).
*   **Контейнеризация:** Docker и Docker Compose для развертывания.
*   **Веб-сервер:** Nginx

## 📂 Структура проекта
В репозитории выделены следующие ключевые модули и файлы:
*   `my_blog/`: Основная директория с настройками Django-проекта.
*   `posts/`: Модуль, отвечающий за создание, хранение и отображение публикаций.
*   `users/`: Система управления пользователями, профилями и аутентификацией.
*   `templates/` и `static/`: HTML-шаблоны и статические ресурсы проекта.
*   `media/`: Папка для пользовательского контента (изображения, файлы).
*   `db_fill_script.py`: Скрипт для автоматического заполнения базы данных тестовыми данными.
*   `entrypoint.sh`: Скрипт для автоматизации запуска контейнеров.

## 🚀 Инструкция по запуску

### 1. Подготовка окружения
Перед запуском убедитесь, что у вас установлен Docker. 
1. Склонируйте репозиторий.
2. Создайте файл конфигурации: скопируйте содержимое `.env.example` в новый файл `.env` и укажите необходимые параметры.

*Примечание: Вы также можете отслеживать статус выполнения задач напрямую в панели администратора Django.*

### 2. Развертывание всего проекта
Для полного запуска всех сервисов (Django, DB, Nginx) используйте Docker Compose:
```bash
docker-compose up -d --build
```
### 3. Запуск
Перейдите по ссылке:

- http://127.0.0.1/
- http://localhost/

---
